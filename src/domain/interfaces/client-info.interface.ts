/**
 * The client info interface
 * @description This interface is used to store the client info
 */
export interface IClientInfo {
  /**
   * The ip address of the client
   */
  ipAddress?: string;
  /**
   * The user agent of the client
   */
  userAgent?: string;
}
